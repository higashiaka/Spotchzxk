// Helper script that reads the plan JSON and outputs it in a format easy to reference in the frontend.
const fs = require('fs');

const html = `
<html lang="ko"><head><title>치지직 CHZZK</title>... (Truncated for safety but I will use the regex matching on the raw string from the prompt context) Wait, I don't want to copy paste the whole prompt HTML into a string. I can just read it from the chat if possible? No I can't.
I will just parse it in JavaScript right here. 
`
