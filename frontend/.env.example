# Local Vite development proxy target.
VITE_DEV_PROXY_TARGET=https://spotchzxk.xyz

# Optional: value accepted by the production Nginx preview_bypass cookie.
# Put the real value in frontend/.env.local, which is ignored by Git.
VITE_DEV_PREVIEW_BYPASS=
